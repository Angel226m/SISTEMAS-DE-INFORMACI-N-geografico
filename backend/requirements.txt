# ── GeoRiesgo Perú — requirements v4.0 ─────────────────────────
# Sin GDAL/GeoPandas → imagen Docker ~6x más ligera

# API Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
python-multipart==0.0.18

# Base de datos async
asyncpg==0.30.0
psycopg2-binary==2.9.10

# HTTP / Retries
requests==2.32.3
httpx==0.28.1
tenacity==9.0.0

# Geometría ligera (sin GDAL)
shapely==2.0.6
pyproj==3.7.0

# Utilidades
python-dotenv==1.0.1
orjson==3.10.12         # JSON ultrarrápido para GeoJSON grandes